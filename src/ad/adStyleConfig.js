const overlayS = {
  position: 'fixed',
  top: 0,
  left: 0,
  width: '100vw',
  height: '100vh',
  backgroundColor: 'rgba(0, 0, 0, .35)'
}

const adCotentS = {
  position: 'absolute',
  top: '50%',
  left: '50%',
  width: '60%',
  height: '40%',
  transform: 'translate(-50%, -50%)',
  backgroundColor: 'red'
}

const adWrapperS = {
  width: '100%',
  height: '100%'
}

const adBannerS = {
  width: '100%',
  objectPosition: 'center'
}

const imgWrapperS = {
  width: '100%',
  height: '80%'
}

module.exports = {
  overlayS,
  adCotentS,
  adWrapperS,
  adBannerS,
  imgWrapperS
}